<%= f.fields_for :entry, f.object.entry do |ff| %>
  <%= markup do |m|
    p = EntryFormPresenter.new(ff, self)
    p.with_options(required: true) do |q|
      m << q.text_field_block(:body, 'コメント（43文字以内）', size:43 )
    end
  end %>
<% end %>